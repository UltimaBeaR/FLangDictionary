<Window x:Class="FLangDictionary.UI.TwoButtonsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FLangDictionary.UI"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <Grid Margin="10,0,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0, 0, 0, 10" MaxWidth="250" TextWrapping="WrapWithOverflow" TextAlignment="Center" x:Name="message" />
        
        <Button Grid.Row="1" Grid.Column="0" Height="20" HorizontalAlignment="Center" MinWidth="75" x:Name="positiveButton" Click="positiveButton_Click" />
        <Button Grid.Row="1" Grid.Column="1" Height="20" HorizontalAlignment="Center" MinWidth="75" x:Name="negativeButton" Click="negativeButton_Click" />
    </Grid>
</Window>